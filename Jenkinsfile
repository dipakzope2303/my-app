pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Checkout Code'
            }
        }

        stage('Build') {
            steps {
                sh 'pwd'
                sh 'ls -lrt'
            }
        }
    }
}
